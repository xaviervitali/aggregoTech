<form method="post" action="{{ path('collaboration_choice_delete', {'id': collaboration_choice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collaboration_choice.id) }}">
    <button class="btn">Delete</button>
</form>
