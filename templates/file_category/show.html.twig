{% extends 'base.html.twig' %}

{% block title %}FileCategory{% endblock %}

{% block body %}
    <h1>FileCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ file_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ file_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('file_category_index') }}">back to list</a>

    <a href="{{ path('file_category_edit', {'id': file_category.id}) }}">edit</a>

    {{ include('file_category/_delete_form.html.twig') }}
{% endblock %}
