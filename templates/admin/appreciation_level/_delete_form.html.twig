<form method="post" action="{{ path('appreciation_level_delete', {'id': level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ level.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
