{% extends 'base.html.twig' %}

{% block title %}CollaborationChoice{% endblock %}

{% block body %}
    <h1>CollaborationChoice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collaboration_choice.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ collaboration_choice.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collaboration_choice_index') }}">back to list</a>

    <a href="{{ path('collaboration_choice_edit', {'id': collaboration_choice.id}) }}">edit</a>

    {{ include('collaboration_choice/_delete_form.html.twig') }}
{% endblock %}
