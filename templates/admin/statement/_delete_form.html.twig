<form method="post" action="{{ path('statement_delete', {'id': statement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statement.id) }}">
    <button class="btn">Delete</button>
</form>
