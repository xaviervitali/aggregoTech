{% extends 'base.html.twig' %}

{% block title %}HolidayReason{% endblock %}

{% block body %}
    <h1>HolidayReason</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ holiday_reason.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ holiday_reason.name }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ holiday_reason.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('holiday_reason_index') }}">back to list</a>

    <a href="{{ path('holiday_reason_edit', {'id': holiday_reason.id}) }}">edit</a>

    {{ include('holiday_reason/_delete_form.html.twig') }}
{% endblock %}
