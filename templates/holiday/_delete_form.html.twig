<form method="post" action="{{ path('holiday_delete', {'id': holiday.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ holiday.id) }}">
    <button class="btn">Delete</button>
</form>
