<form method="post" action="{{ path('collaboration_delete', {'id': collaboration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collaboration.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
